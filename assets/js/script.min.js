window.innerWidth < 768 &&
  [].slice
    .call(document.querySelectorAll("[data-bss-disabled-mobile]"))
    .forEach(function (i) {
      i.classList.remove("animated"),
        i.removeAttribute("data-bss-hover-animate"),
        i.removeAttribute("data-aos"),
        i.removeAttribute("data-bss-parallax-bg"),
        i.removeAttribute("data-bss-scroll-zoom");
    }),
  document.addEventListener(
    "DOMContentLoaded",
    function () {
      "AOS" in window && AOS.init();
    },
    !1
  ),
  jQuery(document).ready(function (s) {
    var e,
      a,
      t = 2500,
      n = 3800;
    function d() {
      s(window).width(), s(".is-visible").css("color", "#0d6efd");
    }
    function o(i) {
      var s = u(i);
      if (i.parents(".cd-headline").hasClass("type")) {
        var e = i.parent(".cd-words-wrapper");
        e.addClass("selected").removeClass("waiting"),
          setTimeout(function () {
            e.removeClass("selected"),
              i
                .removeClass("is-visible")
                .addClass("is-hidden")
                .children("i")
                .removeClass("in")
                .addClass("out");
          }, 500),
          setTimeout(function () {
            l(s, 150);
          }, 1300);
      } else if (i.parents(".cd-headline").hasClass("letters")) {
        var a = i.children("i").length >= s.children("i").length;
        r(i.find("i").eq(0), i, a, 50), c(s.find("i").eq(0), s, a, 50);
      } else
        i.parents(".cd-headline").hasClass("clip")
          ? i
              .parents(".cd-words-wrapper")
              .animate({ width: "2px" }, 600, function () {
                h(i, s), l(s);
              })
          : i.parents(".cd-headline").hasClass("loading-bar")
          ? (i.parents(".cd-words-wrapper").removeClass("is-loading"),
            h(i, s),
            setTimeout(function () {
              o(s);
            }, n),
            setTimeout(function () {
              i.parents(".cd-words-wrapper").addClass("is-loading");
            }, 800))
          : (h(i, s),
            setTimeout(function () {
              o(s);
            }, t));
    }
    function l(i, s) {
      i.parents(".cd-headline").hasClass("type")
        ? (c(i.find("i").eq(0), i, !1, s),
          i.addClass("is-visible").removeClass("is-hidden"))
        : i.parents(".cd-headline").hasClass("clip") &&
          i
            .parents(".cd-words-wrapper")
            .animate({ width: i.width() + 10 }, 600, function () {
              setTimeout(function () {
                o(i);
              }, 1500);
            });
    }
    function r(i, e, a, n) {
      if (
        (i.removeClass("in").addClass("out"),
        i.is(":last-child")
          ? a &&
            setTimeout(function () {
              o(u(e));
            }, t)
          : setTimeout(function () {
              r(i.next(), e, a, n);
            }, n),
        i.is(":last-child") && s("html").hasClass("no-csstransitions"))
      ) {
        var d = u(e);
        h(e, d);
      }
    }
    function c(i, s, e, a) {
      i.addClass("in").removeClass("out"),
        i.is(":last-child")
          ? (s.parents(".cd-headline").hasClass("type") &&
              setTimeout(function () {
                s.parents(".cd-words-wrapper").addClass("waiting");
              }, 200),
            e ||
              setTimeout(function () {
                o(s);
              }, t))
          : setTimeout(function () {
              c(i.next(), s, e, a);
            }, a);
    }
    function u(i) {
      return i.is(":last-child") ? i.parent().children().eq(0) : i.next();
    }
    function h(i, s) {
      i.removeClass("is-visible").addClass("is-hidden"),
        s.removeClass("is-hidden").addClass("is-visible");
    }
    s(".cd-headline.letters")
      .find("b")
      .each(function () {
        var e = s(this),
          a = e.text().split(""),
          t = e.hasClass("is-visible");
        for (i in a)
          e.parents(".rotate-2").length > 0 && (a[i] = "<em>" + a[i] + "</em>"),
            (a[i] = t
              ? '<i class="in">' + a[i] + "</i>"
              : "<i>" + a[i] + "</i>");
        var n = a.join("");
        e.html(n).css("opacity", 1);
      }),
      (e = s(".cd-headline")),
      (a = t),
      e.each(function () {
        var i = s(this);
        if (i.hasClass("loading-bar"))
          (a = n),
            setTimeout(function () {
              i.find(".cd-words-wrapper").addClass("is-loading");
            }, 800);
        else if (i.hasClass("clip")) {
          var e = i.find(".cd-words-wrapper"),
            t = e.width() + 8;
          e.css("width", t);
        } else if (!i.hasClass("type")) {
          var d = i.find(".cd-words-wrapper b"),
            l = 0;
          d.each(function () {
            var i = s(this).width();
            i > l && (l = i);
          }),
            i.find(".cd-words-wrapper").css("width", l);
        }
        setTimeout(function () {
          o(i.find(".is-visible").eq(0));
        }, a);
      }),
      s(document).ready(function () {
        d(),
          s(window).resize(function () {
            d();
          });
      });
  }),
  $(document).ready(function () {
    var i = $("#navigation"),
      s = i.position().top;
    $(window).resize(function () {
      s = i.position().top;
    }),
      $(window).scroll(function () {
        $(this).scrollTop() > s
          ? (i.addClass("sticky"), $(".logosmall").removeClass("hidels"))
          : (i.removeClass("sticky"), $(".logosmall").addClass("hidels"));
      }),
      $(".navResponsive").click(function () {
        $("nav ul.navHold").toggle();
      }),
      $("nav ul li.subNav").click(function () {
        $(this).children("ul").toggle();
      });
  }),
  $(".carousel").on("slide.bs.carousel", function (i) {
    $(this)
      .find(".carousel-inner")
      .animate({ height: $(i.relatedTarget).height() }, 500);
  });

let mybutton = document.getElementById("myBtn");

// When the user scrolls down 20px from the top of the document, show the button
window.onscroll = function () {
  scrollFunction();
};

function scrollFunction() {
  if (document.body.scrollTop > 20 || document.documentElement.scrollTop > 20) {
    mybutton.style.display = "block";
  } else {
    mybutton.style.display = "none";
  }
}

// When the user clicks on the button, scroll to the top of the document
function topFunction() {
  document.body.scrollTop = 0; // For Safari
  document.documentElement.scrollTop = 0; // For Chrome, Firefox, IE and Opera
}
